export default interface QuestionParagraphPropsType {
  text?: string;
  isCenter?: boolean;
  // 加粗
  isStrong?: boolean;
  // 斜体
  isItalic?: boolean;
  isCode?: boolean;
  fontSize?: number;
  textColor?: string;
}
